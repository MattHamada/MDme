<div class='row'>
  <div class='col-md-8'>
<%= form_for(@appointment, remote: true, html: { class: "form-inline", role: "form" }) do |f| %>
        <div class="form-group">
          <% #TODO wrap each generated datetime select in form-group %>
          <%= select_datetime(Time.now, {}, {class: 'form-control'}) %>
        </div>

    <%= f.submit 'Create', class: 'btn btn-success' %>

<% end %>

 </div>
</div>
