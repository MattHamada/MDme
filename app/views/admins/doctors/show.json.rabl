child @doctor, :object_root => false do
  attributes :id,
             :full_name,
             :avatar_thumb_url,
             :email,
             :department_name,
             :phone,
             :description
end
