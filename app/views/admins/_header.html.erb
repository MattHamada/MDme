<div class="container-fluid">
  <%= render 'shared/flash' %>
  <div class="row">
    <div class="col-md-3">
      <div class="breadcrumbs">
        <%= render_breadcrumbs  separator: ' > ' %>
      </div>
    </div>
    <div class="col-md-3 col-md-offset-6" style="text-align: right;">
      <%= link_to 'Sign Out', session_path(current_admin), method: :delete %>
    </div>
  </div>
  <div class="row">
    <div class="col-md-6 col-md-offset-3" style="text-align: center;">
      <%= link_to image_tag(asset_path('full_logo_white.png'), class: 'logo img-responsive'), root_path %>

    </div>
  </div>
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
      <hr id="menu-topbar-black"/>
    </div>
  </div>
  <div class="row" style="margin-bottom: 9em;">
    <div class="col-md-10 col-md-offset-1">
      <div class="row top-links home-menu-links">
        <div class="col-xs-2 col-md-2 col-xs-offset-1 col-md-offset-1 <%= 'top-links-active' if @active == :appointments %>">
          <%= link_to 'APPOINTMENTS', admin_appointments_path(current_admin) %>
        </div>
        <div class="col-xs-2 col-md-2 <%= 'top-links-active' if @active == :doctors %>">
          <%= link_to 'DOCTORS', admin_doctors_path(current_admin) %>
        </div>
        <div class="col-xs-2 col-md-2 <%= 'top-links-active' if @active == :patients %>">
          <%= link_to 'PATIENTS', admin_patients_path(current_admin) %>
        </div>
        <div class="col-xs-2 col-md-2 <%= 'top-links-active' if @active == :departments %>">
          <%= link_to 'DEPARTMENTS', admin_departments_path(current_admin) %>
        </div>
         <div class="col-xs-2 col-md-xs-2 <%= 'top-links-active' if @active == :administration %>">
           <%= link_to 'ADMINISTRATION', admin_clinic_path(current_admin, current_admin.clinic) %>
         </div>
      </div>
    </div>
  </div>
</div>
